# ignore all pdfs in the top level (assume they're auto-generated)
*.pdf

# but keep the PDFs needed for releases
!handouts/out/modules.pdf
!handouts/out/worksheets/grid.pdf

data/

# but keep frankenstein.txt for the release
!data/frankenstein.txt

# default model name
model.json

# output directory for generated files
out/

# Added by cargo
/target
cli/target/
